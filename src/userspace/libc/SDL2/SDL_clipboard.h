#ifndef SDL_clipboard_h_
#define SDL_clipboard_h_

/* Clipboard functions are declared in SDL.h on this shim. This header exists
 * only so ScummVM's `#include <SDL_clipboard.h>` resolves. */
#include "SDL.h"

#endif
